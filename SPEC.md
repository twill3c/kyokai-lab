# SPEC.md — kyokai-lab

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

2 次元データセット(XOR・同心円・二重渦巻き・市松)に小さな多層パーセプトロンを
ブラウザ内で学習させ、決定境界が変形していく過程をリアルタイム可視化する教材アプリ。
逆伝播の勾配は数値微分(中心差分)との一致で検証し、「学習器の正しさ」をテストで保証する。
ブラウザ完結・静的エクスポート。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | シード付き PRNG(mulberry32)を注入。重み初期化・データ生成は同一シードで決定論 | must |
| F-02 | データセット 4 種: XOR / 同心円 / 二重渦巻き / 市松(各 200 点・座標 [-1,1]²・シード決定的・ラベル 2 値) | must |
| F-03 | MLP: 隠れ層 1〜3・各層 2〜16 ニューロン・活性化 tanh / ReLU / sigmoid。出力はシグモイド 1 ユニット・損失は二値クロスエントロピー | must |
| F-04 | 学習は全バッチ勾配降下(決定論)。`trainStep(net, data, lr)` は純関数。学習率は実行中変更可で次ステップから反映 | must |
| F-05 | 可視化: 決定境界ヒートマップ(出力確率 0..1 を発散配色でグリッド描画)+ データ点オーバーレイ(正誤で縁取り) | must |
| F-06 | 実行制御: 再生 / 一時停止 / 1 ステップ / リセット / 速度 1x・10x・50x | must |
| F-07 | アーキテクチャ編集: 隠れ層の追加・削除、各層ニューロン数、活性化関数の切替。変更時は再初期化(keyed remount) | must |
| F-08 | 損失曲線(対数軸)と訓練精度 % の常時表示 | must |
| F-09 | フッタ: MIT License・GitHub・操作説明(鍛え方)・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。サーバ API・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ・決定論。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 描画(rAF)と学習ステップを分離。1 フレームの学習バッチに上限 | フレーム内ステップ上限のテスト |
| N-04 | ポインタ・タッチ両対応(ボタン・スライダー・セレクトのみで操作完結) | 手動確認 |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

学習ゲート(シード固定・ステップ予算付き。予算・閾値は 3 シード較正実験で確定済み):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 勾配検証 | 逆伝播の全勾配が中心差分(ε=1e-5)と相対誤差 < 1e-4 で一致(tanh/ReLU/sigmoid × 複数構成) |
| G-02 | XOR 収束 | シード 1、隠れ [4]・tanh・lr 0.5、2,000 ステップ後に訓練精度 = 100% |
| G-03 | 同心円収束 | シード 1、隠れ [8]・tanh・lr 0.5、3,000 ステップ後に訓練精度 ≥ 98% |
| G-04 | 決定論 | 同一シード・同一構成で 2 回学習した重みが深い等値 |
| G-05 | 損失の実質減少 | G-02/G-03 の学習で最終損失 < 初期損失 × 0.1 |

註(kyoka-grid の教訓の適用): 確率的でない全バッチ勾配降下でも、「損失が毎ステップ単調減少」は
固定学習率では保証されない(谷を跳ねる)。ゲートは終端の行動的基準(精度・損失の桁)で判定する。
渦巻き・市松は収束予算が大きく実行時間が読めないため、テストゲートにせず UI 上の挑戦課題とする。

## 5. スコープ外

- 確率的ミニバッチ・Adam 等の最適化手法(全バッチ GD のみ。将来ループ候補)
- 多クラス分類・回帰(2 値分類のみ)
- ユーザーによる点の追加編集・データセットのアップロード
- 学習結果の永続化・効果音
