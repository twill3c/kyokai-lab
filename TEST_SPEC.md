# TEST_SPEC.md — kyokai-lab

<!-- scaffold template v1.8.0 から展開(2026-08-17) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用。カバレッジ判定は `npx vitest run --coverage`
- テストは `src/core/__tests__/*.test.ts`(+ `src/lib/*.test.ts`)に置き、describe に対応 T-ID をコメントで残す
- 数値微分オラクル(T-03x)は**テスト内で独立実装**する(core の backward を検証する側が
  core の実装を流用しない)。前提(ε の選び方・相対誤差の定義)をコメントに残す
- 学習ゲート(T-1xx)はシード固定・ステップ予算付き。期待値を較正実験なしに書かない(VERIF-FALSE 予防)

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | mulberry32 同一シード | 同一列・[0,1) |
| T-002 | F-01 | 異なるシード | 先頭 8 個が一致しない |
| T-010 | F-02 | 全データセット生成(シード 1) | 各 200 点・座標 [-1,1]²・両ラベル存在・同一シードで深い等値 |
| T-011 | F-02 | XOR の整合 | ラベルが象限と一致(x·y > 0 ⇔ label 1) |
| T-012 | F-02 | 同心円の整合 | 内円は label 1・外輪は label 0(半径で検算) |
| T-020 | F-03 | initNet | 層形状が spec どおり・重みが全ゼロでない・同一シードで再現 |
| T-021 | F-03 | forward の手計算一致 | 1×1 の恒等的極小ネットで σ(w·a(v·x+b)+c) を手計算と照合 |
| T-022 | F-03 | 損失 BCE | 完全正解で ≈0・確率 0.5 で ln2・クランプで有限 |
| T-030 | G-01 | 勾配検証 tanh/ReLU/sigmoid × 構成 [3]/[4,3]/[5,4,3] | 中心差分(ε=1e-5)との相対誤差 < 1e-4(ReLU は非微分点を避けた入力) |
| T-040 | F-04 | trainStep 純関数性 | 入力 net 非破壊・同一入力で同一出力 |
| T-041 | F-04 | lr の反映 | lr を半分にすると重み更新量も半分 |
| T-050 | F-05 | boundaryGrid | 解像度どおりの確率格子・値域 [0,1] |
| T-051 | F-05 | probColor | p=0.5 で中立色・0/1 で各極・単調・#rrggbb |
| T-052 | F-08 | 損失曲線 points 生成(対数軸) | 正値のみ受理・単調写像・空列は空文字 |
| T-060 | N-03 | batchSize | 全速度で上限以下・1x は 1 |
| T-080 | F-09 | FOOTER_LINKS | 5 件・全 https・ラベル非空・育て方≠設計図 |
| T-100 | G-02/G-05 | XOR: シード 1・[6]・tanh・lr 0.5・4,000 ステップ | 訓練精度 100% かつ最終損失 < 初期損失 × 0.1 |
| T-101 | G-03/G-05 | 同心円: シード 1・[8]・tanh・lr 0.5・3,000 ステップ | 訓練精度 100% かつ最終損失 < 初期損失 × 0.1 |
| T-102 | G-04 | 同一シードで 2 回学習(XOR 500 ステップ) | 重み・損失履歴が深い等値 |
